# TAGS ONLY. `make seed` resolves each to a digest, writes infra/images.lock, and
# PUSHES each into the local registry — BuildKit re-resolves FROM on every build
# and cannot see the daemon's cache, so pulling alone is not enough (S1).
node:22-alpine
alpine:3.22
mongodb/mongodb-community-server:7.0.28-ubi8
curlimages/curl:8.11.1
# The §12 supply-chain scanners. They are mirrored as `base/syft` and `base/grype`
# like everything else here, which is a slight misnomer -- nothing pulls them FROM
# the registry, the daemon runs them by tag from its own store. They are in this
# list for the two things it buys: a digest in infra/images.lock, and an offline
# `make reset` + re-seed that still has them.
anchore/syft:v1.51.1
anchore/grype:v0.118.0

# LiteLLM, AND IT IS PINNED BY DIGEST RATHER THAN BY TAG — the only line here
# that is. `ghcr.io/berriai/litellm:main-stable` is a MOVING tag: measured
# 2026-09-07 it resolved to sha256:20b5044b (litellm 1.98.0, built 08-22), and
# on 2026-09-09 a rebuild moved it to sha256:a3715fa7. Two days.
#
# §16 pins the AI error mapping to a LiteLLM version, and S3's error table —
# the `detail` shape of a route denial, and the message substring that
# separates a key budget from an end-user budget — was measured against 1.98.0.
# So the digest is the reference, and moving to a newer LiteLLM is a deliberate
# act with those mapping tests in hand, not something `make seed` does quietly.
# Rich's call, 2026-09-09.
#
# It is NOT mirrored into the local registry (mirror-images.sh skips it):
# nothing does `FROM` this image, so pushing ~2 GB there buys nothing. It is in
# this list for one thing — a digest in infra/images.lock.
ghcr.io/berriai/litellm@sha256:20b5044b619055374061a6d5b7b08754cad75aeabbf82ddf4f69cc0cf80ddaf4
