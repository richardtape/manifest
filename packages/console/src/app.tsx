// Task 4 replaces this with the shell: the header, the nav, the router's switch and the
// refusal surface. It exists now so that `main.tsx` — the only file that touches the DOM
// root — has a caller from the first commit (ORIENTATION §9: a module with no call site is
// not built).
export function App() {
  return <p>Manifest console</p>
}
